# Sunucu Deploy Ayarları
# Bu dosyayı proje kök dizininde .env.deploy olarak kopyalayın ve kendi sunucu bilgilerinizi girin
# cp scripts/deploy.env.example .env.deploy

# Sunucu Bilgileri
DEPLOY_HOST=your-server.com
DEPLOY_USER=root
DEPLOY_PATH=/var/www/fitness-app

# SSH Port (varsayılan: 22)
DEPLOY_SSH_PORT=22

# SSH Key Path (opsiyonel, varsayılan: ~/.ssh/id_rsa)
# DEPLOY_SSH_KEY=~/.ssh/id_rsa

# SSH Şifre ile bağlantı (opsiyonel, güvenlik için key kullanmanız önerilir)
# DEPLOY_USE_PASSWORD=true
# DEPLOY_SSH_PASSWORD=your-password

# Otomatik onay (watch modu için)
# AUTO_CONFIRM=true

# Docker Ayarları (Production)
# Bu değerler sunucudaki .env dosyasında da olmalı
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=fitness_app
POSTGRES_PORT=5432
APP_PORT=3000

# NextAuth Ayarları (Production)
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your-nextauth-secret-here

# Watch Modu Ayarları
# Deploy gecikmesi (saniye) - çoklu değişikliklerde tek deploy için
WATCH_DEPLOY_DELAY=5

# Not: SSH key-based authentication kullanmanız önerilir
# SSH key oluşturmak için: ssh-keygen -t rsa -b 4096
# Sunucuya key kopyalamak için: ssh-copy-id user@host

